import { Box, Typography } from '@mui/material';

export const MoodTracker = () => {
  return (
    <Box>
      <Typography variant="h4">Mood Tracker</Typography>
      {/* We'll implement the mood tracking interface here */}
    </Box>
  );
};
