import { Box, Typography } from '@mui/material';

export const DailyRoutine = () => {
  return (
    <Box>
      <Typography variant="h4">Daily Routine</Typography>
      {/* Daily routine tracking interface will be implemented here */}
    </Box>
  );
};
